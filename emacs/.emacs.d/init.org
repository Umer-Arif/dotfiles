# -*- after-save-hook: (org-babel-tangle); before-save-hook: (delete-trailing-whitespace) -*-
#+TITLE: Emacs Org Config File
#+AUTHOR: Omer Arif
#+STARTUP: overview
#+PROPERTY: header-args:emacs-lisp :exports code :results none :tangle ~/.emacs.d/init.el

* Initialization
#+BEGIN_SRC emacs-lisp

  ;; Auto-save and Backup Configuration
  (make-directory "~/.emacs.d/auto-save-list" t)
  (setq backup-directory-alist `(("." . "~/.emacs.d/auto-save-list")))
  (setq auto-save-file-name-transforms `((".*" "~/.emacs.d/auto-save-list" t)))
  (setq auto-save-list-file-prefix "~/.emacs.d/auto-save-list/.saves-")
  (setq version-control t)
  (setq kept-new-versions 10)
  (setq kept-old-versions 2)
  (setq delete-old-versions t)
  (put 'after-save-hook 'safe-local-variable #'listp)
  (put 'before-save-hook 'safe-local-variable #'listp)


#+END_SRC

* UI
#+BEGIN_SRC emacs-lisp

  ;; --- Don't show splash screen ------------------------------------------------
  (setq-default inhibit-startup-message t)

  ;; --- Flash on errors instead of making a sound -------------------------------
  (setq-default visible-bell t)

  ;; --- Wrap lines at 100 characters --------------------------------------------
  (setq-default fill-column 100)

  ;; --- Set default tab width to 4 spaces ---------------------------------------
  (setq-default tab-width 4)

  ;; --- Set custom greeting message for the initial scratch buffer --------------
  (setq-default initial-scratch-message ";; WELCOME TO EMACS! \n\n")

  ;; --- Turn off the menu bar at top of window ----------------------------------
  (menu-bar-mode -1)

  ;; --- Turn off the toolbar with icons -----------------------------------------
  (tool-bar-mode -1)

  ;; --- Turn off the scroll bar on the side of the window -----------------------
  (scroll-bar-mode -1)

  ;; --- Replace "yes/no" prompts with shorter "y/n" prompts ---------------------
  (fset 'yes-or-no-p 'y-or-n-p)

  ;; --- When you type, replace selected text instead of inserting before it ------
  (delete-selection-mode 1)

  ;; --- Force Emacs to free up unused memory ------------------------------------
  (defun me/garbage-collect-maybe ()
    (garbage-collect))

  ;; --- Run garbage collection when you leave the Emacs window ------------------
  (add-hook 'focus-out-hook #'me/garbage-collect-maybe)

  ;; --- Make frame 90% transparent ----------------------------------------------
  (add-to-list 'default-frame-alist '(alpha-background . 0.9))

  ;; --- Auto-trim trailing spaces when saving without asking confirmation --------
  (add-to-list 'safe-local-variable-values
               '(before-save-hook . delete-trailing-whitespace))

  ;; --- Remember cursor position in files when you reopen them ------------------
  (save-place-mode 1)

  ;; --- Keep cursor position even for files on USB drives or network shares -----
  (setq save-place-forget-unreadable-files nil)

  ;; --- Store the cursor position data in a custom file inside Emacs config folder
  (setq save-place-file (expand-file-name "places" user-emacs-directory))

  ;; --- Update the saved cursor position immediately when you save a file --------
  (add-hook 'after-save-hook 'save-place-to-alist)

  ;; --- Show column number in mode line -----------------------------------------
  (column-number-mode t)

  ;; --- Show line numbers for every buffer by default ---------------------------
  (global-display-line-numbers-mode t)

  ;; --- Highlight the current line with a background color ----------------------
  (global-hl-line-mode 1)

  ;; --- Show line numbers as distance from current line -------------------------
  (setq display-line-numbers-type 'relative)

  ;; --- Turn off line numbers for terminal/shell and Org buffers ----------------
  (dolist (mode '(org-mode-hook
                  org-agenda-mode-hook
                  term-mode-hook
                  shell-mode-hook
                  eshell-mode-hook
                  vterm-mode-hook
                  eat-mode))
    (add-hook mode (lambda () (display-line-numbers-mode 0))))

  ;; --- Tell Emacs to use UTF-8 for language settings ---------------------------
  (set-language-environment "UTF-8")

  ;; --- Prefer UTF-8 when reading/writing files ---------------------------------
  (prefer-coding-system 'utf-8)

  ;; --- Use UTF-8 for copy/paste with system clipboard --------------------------
  (set-selection-coding-system 'utf-8)

  ;; --- Use UTF-8 for keyboard input --------------------------------------------
  (set-keyboard-coding-system 'utf-8)

  ;; --- Save files as UTF-8 with Unix line endings ------------------------------
  (setq default-buffer-file-coding-system 'utf-8-unix)

  ;; --- Make cursor blink -------------------------------------------------------
  (blink-cursor-mode 1)

#+END_SRC
** Font
#+BEGIN_SRC emacs-lisp
  (set-face-attribute 'default nil :font "Iosevka" :height 150)
#+END_SRC
* Clipboard
#+BEGIN_SRC emacs-lisp
  (setq select-enable-clipboard t)  ; Sync Emacs kill ring with system clipboard (copy/paste works with other apps)

  (setq async-shell-command-buffer "confirm-kill-process")  ; Ask before killing a running shell command when you close its buffer
#+END_SRC
* Themes
#+BEGIN_SRC emacs-lisp
  ;; Treat all custom themes as safe to prevent Emacs from prompting you on startup
  (setq custom-safe-themes t)

  ;; Add your custom theme directory to Emacs's theme search path
  (add-to-list 'custom-theme-load-path "~/.emacs.d/themes/t1-emacs-themes")

  ;; Load the specific dark theme flavor
  (load-theme 'omer-kaolin-dark)

#+END_SRC
* Keybinds
** Directory navigation
#+BEGIN_SRC emacs-lisp

      ;; For init.org
    (defun my/open-init-org ()
      "Open the init.org file."
      (interactive)
      (find-file "~/dotfiles/emacs/.emacs.d/init.org"))

    (global-set-key (kbd "C-c i") 'my/open-init-org)


  ;; Open ~/Documents/org directory
  (defun my/open-org-directory ()
    "Open the ~/Documents/org directory in dired."
    (interactive)
    (dired "~/Documents/org"))

  (global-set-key (kbd "C-c C-o") 'my/open-org-directory)

  ;;Open dotfiles directory
  (defun my/open-dotfiles ()
    "Open the ~/dotfiles directory in dired."
    (interactive)
    (dired "~/dotfiles"))

  (global-set-key (kbd "C-c d") 'my/open-dotfiles)

#+END_SRC

** Code Execution
#+BEGIN_SRC emacs-lisp

  ;; --- Python Execution (C-c p) ------------------------------------------------
  (defun omer/run-python ()
    "Run current Python file."
    (interactive)
    (unless (string= (file-name-extension (buffer-file-name)) "py")
      (user-error "Not a Python file"))
    (save-buffer)
    (python-shell-send-buffer)
    (python-shell-switch-to-shell))

  (global-set-key (kbd "C-c p") #'omer/run-python)

  ;; --- C Compilation & Execution (C-c c) ---------------------------------------
  (defun omer/run-c ()
    "Compile and run C code."
    (interactive)
    (unless (string= (file-name-extension (buffer-file-name)) "c")
      (user-error "Not a C file"))
    (save-buffer)
    (let ((output (file-name-sans-extension (buffer-file-name))))
      ;; Compile and execute via async shell to avoid blocking Emacs
      (async-shell-command
       (format "gcc %s -o %s && %s"
               (shell-quote-argument (buffer-file-name))
               (shell-quote-argument output)
               (shell-quote-argument output))
       "*C Output*")
      (switch-to-buffer-other-window "*C Output*")))

  (global-set-key (kbd "C-c c") #'omer/run-c)

  ;; --- Rust Execution via vterm (C-c r) ----------------------------------------
  (defun omer/run-rust ()
    "Run `cargo run` for current Rust project."
    (interactive)
    (unless (string= (file-name-extension (buffer-file-name)) "rs")
      (user-error "Not a Rust file"))
    (save-buffer)
    ;; Find project root directory containing Cargo.toml
    (let ((default-directory
           (or (locate-dominating-file default-directory "Cargo.toml")
               (user-error "Not inside a Cargo project"))))
      (let ((buf (get-buffer-create "*Rust Terminal*")))
        (switch-to-buffer-other-window buf)
        (unless (derived-mode-p 'vterm-mode)
          (vterm-mode))
        (vterm-send-string "cargo run")
        (vterm-send-return))))

  (global-set-key (kbd "C-c r") #'omer/run-rust)

  ;; --- Lua Execution (C-c l) ---------------------------------------------------
  (defun omer/run-lua ()
    "Run current Lua file."
    (interactive)
    (unless (string= (file-name-extension (buffer-file-name)) "lua")
      (user-error "Not a Lua file"))
    (save-buffer)
    (async-shell-command
     (format "lua %s" (shell-quote-argument (buffer-file-name)))
     "*Lua Output*")
    (switch-to-buffer-other-window "*Lua Output*"))

  (global-set-key (kbd "C-c l") #'omer/run-lua)

  ;; --- Common Lisp Evaluation via SLY (C-c e) ----------------------------------
  (defun omer/run-lisp ()
    "Evaluate Common Lisp buffer using SLY."
    (interactive)
    (unless (derived-mode-p 'lisp-mode)
      (user-error "Not a Common Lisp buffer"))
    (sly-eval-buffer))

  (global-set-key (kbd "C-c e") #'omer/run-lisp)


#+END_SRC
** Buffer
#+BEGIN_SRC emacs-lisp
    ;; WARNING: Muscle Memory Swap!
  (global-set-key (kbd "C-x C-s") 'isearch-forward) ; Swapped: Search forward
  (global-set-key (kbd "C-s") 'save-buffer)         ; Swapped: Save current buffer

  ;; Buffer Management
  (global-set-key (kbd "C-<tab>") #'switch-to-buffer)
  (global-set-key (kbd "C-x C-b") 'ibuffer)           ; Open advanced buffer menu

  ;; ---  DIRED MODE CUSTOMIZATION -------------------------------------------------------
  (defun my/dired-open-file ()
    "Open file at point. Use async external viewer for media/docs, normal for text files."
    (interactive)
    (let* ((file (dired-get-filename nil t))
           (ext (and file (file-name-extension file))))
      (if (and ext (member (downcase ext)
                           '("pdf" "mp4" "mkv" "avi" "mov" "mp3" "flac"
                             "wav" "ogg" "m4a" "webm" "doc" "docx"
                             "xls" "xlsx" "ppt" "pptx")))
          (start-process "dired-open" nil "xdg-open" file)
        (dired-find-file))))

  ;; Bind smart opener to RET (Enter) in Dired
  (with-eval-after-load 'dired
    (define-key dired-mode-map (kbd "RET") 'my/dired-open-file))

  ;; NOTE: This will disable standard Emacs help menus bound to C-h!
  (keyboard-translate ?\C-h ?\C-?)

#+END_SRC
* Package manager
#+BEGIN_SRC emacs-lisp

    (defvar elpaca-installer-version 0.12)
  (defvar elpaca-directory (expand-file-name "elpaca/" user-emacs-directory))
  (defvar elpaca-builds-directory (expand-file-name "builds/" elpaca-directory))
  (defvar elpaca-sources-directory (expand-file-name "sources/" elpaca-directory))
  (defvar elpaca-order '(elpaca :repo "https://github.com/progfolio/elpaca.git"
                                :ref nil :depth 1 :inherit ignore
                                :files (:defaults "elpaca-test.el" (:exclude "extensions"))
                                :build (:not elpaca-activate)))
  (let* ((repo  (expand-file-name "elpaca/" elpaca-sources-directory))
         (build (expand-file-name "elpaca/" elpaca-builds-directory))
         (order (cdr elpaca-order))
         (default-directory repo))
    (add-to-list 'load-path (if (file-exists-p build) build repo))
    (unless (file-exists-p repo)
      (make-directory repo t)
      (when (<= emacs-major-version 28) (require 'subr-x))
      (condition-case-unless-debug err
          (if-let* ((buffer (pop-to-buffer-same-window "*elpaca-bootstrap*"))
                    ((zerop (apply #'call-process `("git" nil ,buffer t "clone"
                                                    ,@(when-let* ((depth (plist-get order :depth)))
                                                        (list (format "--depth=%d" depth) "--no-single-branch"))
                                                    ,(plist-get order :repo) ,repo))))
                    ((zerop (call-process "git" nil buffer t "checkout"
                                          (or (plist-get order :ref) "--"))))
                    (emacs (concat invocation-directory invocation-name))
                    ((zerop (call-process emacs nil buffer nil "-Q" "-L" "." "--batch"
                                          "--eval" "(byte-recompile-directory \".\" 0 'force)")))
                    ((require 'elpaca))
                    ((elpaca-generate-autoloads "elpaca" repo)))
              (progn (message "%s" (buffer-string)) (kill-buffer buffer))
            (error "%s" (with-current-buffer buffer (buffer-string))))
        ((error) (warn "%s" err) (delete-directory repo 'recursive))))
    (unless (require 'elpaca-autoloads nil t)
      (require 'elpaca)
      (elpaca-generate-autoloads "elpaca" repo)
      (let ((load-source-file-function nil)) (load "./elpaca-autoloads"))))
  (add-hook 'after-init-hook #'elpaca-process-queues)
  (elpaca `(,@elpaca-order))

  ;; Install use-package support
(elpaca elpaca-use-package
  ;; Enable use-package :ensure support for Elpaca.
  (elpaca-use-package-mode))

#+END_SRC

* Packages
#+BEGIN_SRC emacs-lisp

  ;; --- Save minibuffer history across Emacs sessions so you can reuse past inputs
  (use-package savehist
    :init
    (savehist-mode 1))

  ;; --- Provide a lightweight, vertical completion interface for the minibuffer --
  (use-package vertico
    :ensure t
    :init
    (vertico-mode)
    (setq vertico-cycle t)
    :config
    (setq vertico-sort-function #'vertico-sort-history-alpha))

  ;; --- Add clean UI descriptions and annotations next to items in completion lists
  (use-package marginalia
    :ensure t
    :config
    (marginalia-mode))

  ;; --- Enable flexible, space-separated, out-of-order pattern matching ---------
  (use-package orderless
    :ensure t
    :config
    (setq completion-styles '(orderless basic)))


  ;; --- Preview lines dynamically in a buffer window before jumping -------------
  (use-package goto-line-preview
    :ensure t
    :bind ("C-x C-j" . 'goto-line-preview))

  ;; --- Edit files with root/sudo privileges directly inside Emacs ---------------
  (use-package sudo-edit
    :ensure t)

  ;; --- Prevent Dired from spawning a separate buffer window for every folder ---
  (put 'dired-find-alternate-file 'disabled nil)


  ;; --- Provide a core library of icons for graphic-mode setups -----------------
  (use-package all-the-icons
    :if (display-graphic-p)
    :ensure t)

  ;; --- Bring high-quality file and folder icons into Dired lists ----------------
  (use-package all-the-icons-dired
    :ensure t
    :hook (dired-mode . all-the-icons-dired-mode))

  ;; --- Attach colorful filetype icons to your minibuffer completion rows -------
  (use-package all-the-icons-completion
    :ensure t
    :after (marginalia all-the-icons)
    :init
    (all-the-icons-completion-mode))

  ;; --- Add colorful contextual prefix icons to your auto-completion lists -------
  (use-package kind-icon
    :ensure t
    :after corfu
    :custom
    (kind-icon-blend-background t)
    (kind-icon-default-face 'corfu-default)
    :config
    (add-to-list 'corfu-margin-formatters #'kind-icon-margin-formatter))


  ;; --- Colorize matching brackets and parentheses by their nesting level -------
  (use-package rainbow-delimiters
    :ensure t
    :hook (prog-mode . rainbow-delimiters-mode))

  ;; --- Automatically insert, wrap, and delete matching pairs of delimiters -------
  (use-package smartparens
    :ensure t
    :hook ((prog-mode . smartparens-mode)
           (org-mode . smartparens-mode)))

  ;; --- Replace the standard undo system with a visual, branching tree -----------
  (use-package undo-tree
    :ensure t
    :custom
    (undo-tree-auto-save-history t)
    (undo-tree-history-directory-alist
     `((".*" . ,(expand-file-name "undo/" user-emacs-directory))))
    :config
    (make-directory (expand-file-name "undo/" user-emacs-directory) t)
    (global-undo-tree-mode))

  ;; --- Draw vertical guide indicators to clearly show code indentation blocks ---
  (use-package indent-bars
    :ensure t
    :hook (prog-mode . indent-bars-mode)
    :custom
    (indent-bars-starting-column 0))

  ;; --- Render beautiful typographic font ligatures in code ----------------------
  (use-package ligature
    :ensure t
    :config
    (ligature-set-ligatures 't '("www"))
    (ligature-set-ligatures 'eww-mode '("ff" "fi" "ffi"))
    (ligature-set-ligatures 'prog-mode '("-<<" "-<" "-<-" "<--" "<---" "<<-" "<-" "->" "->>" "-->" "--->" "->-" ">-" ">>-"
                                         "=<<" "=<" "=<=" "<==" "<===" "<<=" "<=" "=>" "=>>" "==>" "===>" "=>=" ">=" ">>="
                                         "<->" "<-->" "<--->" "<---->" "<=>" "<==>" "<===>" "<====>" "::" ":::" "__"
                                         "<~~" "</" "</>" "/>" "~~>" "==" "!=" "/=" "~=" "<>" "===" "!==" "!===" "=/=" "=!="
                                         "<:" ":=" "*=" "*+" "<*" "<*>" "*>" "<|" "<|>" "|>" "<." "<.>" ".>" "+*" "=*" "=:"
                                         ":>" "(*" "*)" "/*" "*/" "[|" "|]" "{|" "|}" "++" "+++" "\\/" "/\\" "|-" "-|" "<!--" "<!---"))
    (global-ligature-mode))


  ;; --- Provide a keyboard-driven prefix/popup menu framework -------------------
  (use-package transient
    :ensure t)

  ;; --- Enable comprehensive syntax highlighting and tools for Markdown ----------
  (use-package markdown-mode
    :ensure t
    :mode ("README\\.md\\'" . gfm-mode)
    :init (setq markdown-command "multimarkdown"))

  ;; --- A legendary, full-featured Git interface directly inside Emacs ------------
  (use-package magit
    :ensure t)

  ;; --- Run a highly performant terminal emulator inside a buffer window ---------
  (use-package vterm
    :ensure t)


  ;; --- Tell the Elpaca package manager to process everything queued up above ----
  (elpaca-process-queues)

#+END_SRC

* Mode-line
#+BEGIN_SRC emacs-lisp
  (use-package doom-modeline
    :ensure t
    :init
    (doom-modeline-mode 1)
    :custom
    ;; 1. File icon on very left
    (doom-modeline-icon t)
    (doom-modeline-major-mode-icon t)

    (doom-modeline-height 30)

    (doom-modeline-bar-width 4)

    ;; 2. Filename with full path after a little gap
    (doom-modeline-buffer-file-name-style 'file-name) ; Show full file path
    (doom-modeline-buffer-modification-icon t)
    ;;
    ;; 3. Right side: Total lines : Current line : Column
    (doom-modeline-percent-position nil)     ; Disable percentage
    (doom-modeline-total-line-number t)      ; Show total lines
    (doom-modeline-line-number-style 'total) ; Format as "total:current:column"
    (doom-modeline-number-limit nil)         ; Always show line numbers

    ;; Remove all unwanted elements to make space for line info
    (doom-modeline-buffer-encoding nil)
    (doom-modeline-checker-simple-format t)
    (doom-modeline-mu4e nil)
    (doom-modeline-gnus nil)
    (doom-modeline-irc nil)
    (doom-modeline-enable-word-count nil)
    (doom-modeline-time nil)                 ; Remove time display
    (doom-modeline-vcs-max-length 0)         ; Remove version control info

    ;;   ;; Spacing and appearance
    (doom-modeline-spc-face-overrides nil)
    (doom-modeline-project-detection 'auto)

    ;; Additional settings for better spacing
    (doom-modeline-indent-width 1)
    (doom-modeline-modal-icon t)
    (doom-modeline-minor-modes nil))         ; Remove minor modes display-line-numbers-type
#+END_SRC

* Org-mode
#+BEGIN_SRC emacs-lisp

  ;; --- Core Engine & Babel Configuration ----------------------------------------
  (use-package org
    :ensure t
    :config
    (org-babel-do-load-languages
     'org-babel-load-languages
     '((emacs-lisp . t))))

  ;; --- Layout & Behavior Defaults ----------------------------------------------
  (setq org-ellipsis "▾"
        org-src-window-setup 'current-window
        org-startup-with-inline-images t
        org-startup-with-latex-preview nil
        org-image-actual-width nil
        org-confirm-babel-evaluate nil
        org-log-into-drawer t)

  ;; --- Font & Interface Trimming -----------------------------------------------
  (with-eval-after-load 'org
    (set-face-attribute 'org-ellipsis nil :inherit 'default :box nil)
    (set-face-attribute 'org-table nil :family "Monospace"))


  ;; --- Modern Minimalist UI Layer ----------------------------------------------
  (use-package org-modern
    :ensure t
    :config
    (setq org-modern-hide-stars 'leading
          org-modern-star nil
          org-auto-align-tags nil
          org-tags-column 0
          org-catch-invisible-edits 'show-and-error
          org-special-ctrl-a/e t
          org-insert-heading-respect-content t
          org-hide-emphasis-markers t
          org-pretty-entities t)
    (setq org-modern-list
          '((?+ . "•")
            (?* . "◆")
            (?1 . "◦")))
    (global-org-modern-mode))

  ;; --- Stylized Typography & Bullets -------------------------------------------
  (use-package org-superstar
    :ensure t
    :after org
    :hook (org-mode . org-superstar-mode)
    :config
    (setq org-superstar-headline-bullets-list '("◉" "○" "✸" "✿" "●" "○" "◆" "◇")
          org-superstar-item-bullet-alist '((?+ . "•") (?- . "•") (?* . "•"))))

  ;; --- Centered Distraction-Free Canvas ----------------------------------------
  (use-package olivetti
    :ensure t
    :hook
    (org-mode . olivetti-mode)
    (org-agenda-mode . olivetti-mode)
    :custom
    (olivetti-body-width 90))

  ;; --- Image Drag-and-Drop Pipeline --------------------------------------------
  (use-package org-download
    :ensure t
    :custom
    (org-download-heading-lvl nil)
    (org-download-image-org-width 350)
    (org-download-timestamp "%Y%m%d-%H%M%S")
    (org-download-screenshot-method
     (cond ((executable-find "grim") "grim -g \"$(slurp)\" %s")
           ((executable-find "maim") "maim -s %s")
           ((executable-find "spectacle") "spectacle -rb %s")))
    (org-download-edit-cmd
     (cond ((executable-find "krita") "krita %s")
           ((executable-find "gimp") "gimp %s")
           ((executable-find "eog") "eog %s")))
    (org-download-method 'directory)
    (org-download-backend 'curl)
    (org-download-display-inline-images t)
    (org-download-annotate-function nil))

  ;; --- Image Capture & Clipboard Keybindings -----------------------------------
  (with-eval-after-load 'org
    (define-key org-mode-map (kbd "C-c C-s") 'org-download-screenshot)
    (define-key org-mode-map (kbd "C-c C-y") 'org-download-yank))


#+END_SRC

* Programming
#+BEGIN_SRC emacs-lisp

  ;; =============================================================================
  ;; TREE-SITTER MAJOR MODE REMAPPING
  ;; =============================================================================

  ;; --- Major Mode Remap Alist --------------------------------------------------
  (setq major-mode-remap-alist
        '((python-mode     . python-ts-mode)
          (rust-mode       . rust-ts-mode)
          (typescript-mode . typescript-ts-mode)
          (js-mode         . js-ts-mode)
          (tsx-mode        . tsx-ts-mode)
          (bash-mode       . bash-ts-mode)
          (c-mode          . c-ts-mode)
          (nix-mode        . nix-ts-mode)
          (c++-mode        . c++-ts-mode)))

  ;; --- Auto Mode File Extensions -----------------------------------------------
  (add-to-list 'auto-mode-alist '("\\.py\\'" . python-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.rs\\'" . rust-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.ts\\'" . typescript-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.tsx\\'" . tsx-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.nix\\'" . nix-ts-mode))


  ;; =============================================================================
  ;; EGLOT LSP CLIENT CONFIGURATION
  ;; =============================================================================

  ;; --- Language Server Programs Registry ---------------------------------------
  (with-eval-after-load 'eglot
    (add-to-list 'eglot-server-programs
                 '(rust-ts-mode . ("rust-analyzer")))
    (add-to-list 'eglot-server-programs
                 '(python-ts-mode . ("pyright-langserver" "--stdio")))
    (add-to-list 'eglot-server-programs
                 '(nix-ts-mode . ("nil")))
    (add-to-list 'eglot-server-programs
                 '((typescript-ts-mode tsx-ts-mode) . ("typescript-language-server" "--stdio"))))


  ;; =============================================================================
  ;; NIXOS ENVRC PATH POLLING AUTOMATION
  ;; =============================================================================

  ;; --- Asynchronous Path Polling -----------------------------------------------
  (defun my/auto-start-eglot-when-path-ready (buffer program-name)
    "Poll the buffer until envrc injects the path containing PROGRAM-NAME, then start Eglot."
    (when (buffer-live-p buffer)
      (with-current-buffer buffer
        (if (executable-find program-name)
            (eglot-ensure)
          (run-with-timer 0.1 nil #'my/auto-start-eglot-when-path-ready buffer program-name)))))

  ;; --- Environment Initialization Selector -------------------------------------
  (defun my/trigger-nix-eglot-sequence ()
    "Initialize envrc and figure out which binary to watch for based on major-mode."
    (envrc-mode 1)
    (let ((binary-name (cond
                        ((derived-mode-p 'rust-ts-mode) "rust-analyzer")
                        ((derived-mode-p 'python-ts-mode) "pyright-langserver")
                        ((derived-mode-p 'typescript-ts-mode) "typescript-language-server")
                        ((derived-mode-p 'nix-ts-mode) "nil")
                        ((derived-mode-p 'tsx-ts-mode) "typescript-language-server"))))
      (when binary-name
        (my/auto-start-eglot-when-path-ready (current-buffer) binary-name))))

  ;; --- Major Mode Initialization Hooks -----------------------------------------
  (add-hook 'rust-ts-mode-hook       #'my/trigger-nix-eglot-sequence)
  (add-hook 'python-ts-mode-hook     #'my/trigger-nix-eglot-sequence)
  (add-hook 'typescript-ts-mode-hook #'my/trigger-nix-eglot-sequence)
  (add-hook 'tsx-ts-mode-hook        #'my/trigger-nix-eglot-sequence)
  (add-hook 'nix-ts-mode-hook        #'my/trigger-nix-eglot-sequence)

#+END_SRC

* Completion

#+BEGIN_SRC emacs-lisp

  ;; --- CORFU COMPLETION OVERLAY UI -----------------------------------------
  (use-package corfu
    :ensure t
    :init
    (global-corfu-mode 1)
    :custom
    (corfu-auto t)
    (corfu-auto-delay 0.1)
    (corfu-auto-prefix 1)
    (corfu-quit-no-match 'separator)
    :bind
    (:map corfu-map
          ("SPC" . corfu-insert-separator)
          ("<escape>" . corfu-quit)))

  ;; --- Modular Backend Injection -----------------------------------------------
  (use-package cape
    :ensure t
    :init
    (add-to-list 'completion-at-point-functions #'cape-file)
    (add-to-list 'completion-at-point-functions #'cape-dabbrev)
    (add-to-list 'completion-at-point-functions #'cape-keyword))

#+END_SRC
